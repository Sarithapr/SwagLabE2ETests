{
  "name": "y",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "report:merge": "mkdir -p cypress/reports/assets && npx mochawesome-merge cypress/reports/assets/*.json > cypress/reports/assets/report.json",
    "report:generate": "npx marge cypress/reports/report.json --reportDir cypress/reports --inline",
    "test": "npm run cy:run && npm run report:merge && npm run report:generate"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^14.5.3"
  },
  "devDependencies": {
    "mocha": "^11.7.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
